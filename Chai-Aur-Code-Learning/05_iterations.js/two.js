const myObj = {
    js: "javascript",
    cpp: "c++",
    swift: "swift by apple",
    rb: "ruby"
}

for (const key in myObj) {
    // console.log(`${key} is shortcut for ${myObj[key]}`)

   
}

// note point --> array ki keys index no. hi hoti hai like 0 1 2 3 4 ...